# Configuração do Banco de Dados SQLite (substituto do PostgreSQL)
DATABASE_URL=sqlite+aiosqlite:///./database/workout.db

# Credenciais do docker-compose.yml adaptadas para SQLite:
# POSTGRES_USER: workout -> DB_USER
# POSTGRES_PASSWORD: workout -> DB_PASSWORD  
# POSTGRES_DB: workout -> DB_NAME
DB_USER=workout
DB_PASSWORD=workout
DB_NAME=workout

# Configurações da aplicação
APP_NAME=WorkoutAPI
DEBUG=True
HOST=0.0.0.0
PORT=8000

# Equivalência Docker -> SQLite
# image: postgres:16-alpine -> SQLite nativo
# container_name: workout_db -> arquivo workout.db
# ports: 5432:5432 -> não necessário (local)